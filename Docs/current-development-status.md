# LA 当前开发状态

更新时间：2026-07-14

> 本文件是项目进度的唯一事实来源。旧状态文档、旧 PRD 或旧开发计划中的“已完成/下一步”描述不再生效。

## 1. 当前结论

- 当前执行阶段：**R0.1 / P0，V1.1 第一版布局未通过产品体验确认，已按老师原始要求修订为 V1.2，等待重新实施**；R2 可编辑检修预方案第一版已在 WSL 落地并通过浏览器主路径验证，R0/R0.1 与 R2 均需在龙芯开发态验证后再标记最终完成。
- 最新评审输入：V1.2 的“顶部沉淀已知信息、中央围绕地点图生成一张当前任务卡、确认后归档再生成下一项”已通过产品体验确认；`r0-spatial-intake-workspace-spec.md` 已升级为 V1.3，并完成地点之后的发生事件、其他现象、依据与手册及现场异常事件全景第一版实现。
- 多 Agent 会诊展示已按 `r0-right-agent-consultation-stream-spec.md` 从中央迁移到右侧辅助区域：中央只保留业务状态，右侧依次展示 Agent 输入、思考、检索依据、阶段结论与交接过程，并支持类似 GPT 的自动跟随滚动。
- 后台案例回流业务已升级为 V1.2 演示导向基线：用一条完整案例完成工程师提交、专家修订和知识发布视频闭环，配合一条建设中案例、10–12 条只读历史摘要及 6–8 条预置知识支撑 PPT 和后台规模展示。
- `case-feedback-video-script-and-data.md` 已冻结七分钟视频主线、唯一完整案例 `CASE-ACP4000-001`、动态知识 `KB-008 V1.0 → V1.1`、主要图谱关系和录制状态；后台信息架构与开发 Spec 必须从该脚本反推。
- 后台案例回流主路径已通过 WSL 验收：工程师侧取消独立案例回流工作台，完整确认、证据链、恢复前后对比、现场材料和上传动作已合并进“检修完成记录”；专家独立登录后可读取同一份上传证据包并继续三步审核。KB-008 V1.1、正式图谱关系、历史筛选、重置确认和知识验证继续有效；龙芯实体机开发态验证仍待执行。
- 工程师与专家入口已完成角色隔离：`worker001` 登录现有一线诊断台，`expert001` 登录独立专家门户；工程师提交后必须退出再登录专家账号，页面内不再提供身份切换。
- 专家账号资料设置轻量版已完成（WSL）：左侧菜单新增“专家设置”，支持姓名、职称、机构、专业方向、审核设备范围和联系方式的本地保存，并同步更新侧栏账号摘要；真实鉴权、密码和后端用户资料不在本轮范围内。
- 2026-07-13 工程师侧页面调整已完成（WSL）：独立“案例回流工作台”不再参与工程师主流程，案例确认、证据链、恢复前后对比和“上传至专家知识库”已合并进“检修完成记录”；上传后原页显示待专家审核，SQLite 可恢复，专家端能读取完整证据包。方案与验收见 `engineer-maintenance-record-feedback-spec.md`。
- 案例回流 UX 已按最新主线重构：工程师在“检修完成记录”原位确认并上传，上传成功后不跳页、不切换角色；专家端继续使用“工程师确认—专家审核—知识发布—应用验证”四步进度。知识验证描述支持编辑，审查证据保存在 `output/case-flow-audit/`。
- V1.1 否决原因：动画仅为简单入场、发生时间是静态卡、已确认信息放在底部轨迹、中央同时保留多张完整卡片，没有形成老师要求的动态业务主线。现有 V1.1 代码只作为试验实现，下一轮需要按 V1.2 重做。
- 当前开发仓库：WSL2 Ubuntu 的 `/home/kevin/projects/LA`。
- Windows `E:\match\LoongArch` 仅作为资料与素材工作区，不作为最新代码源。
- 当前产品仍是离线优先的高保真演示型 MVP，不是真实生产系统。
- 本阶段不接真实大模型、真实图像识别、真实语音识别、图数据库或真实音视频通话。
- R0 新增现场材料入口：先实现图片/视频本地选择与预览，不做服务器上传和真实识别。

## 2. R0 / P0 定义

R0 对应 2026-07-10 Meeting 中第一项 P0 需求的当前落地批次（Meeting 原编号 R1）。详细完成定义以 `r0-dynamic-intake-workbench-spec.md` 为准：把首页到分析诊断之间的旧四页接入流程改造成单页动态接诊工作区。

验收目标：

- 点击“开始接诊”后不能直接进入固定四步页面。
- 中央故障位置图在整个接入阶段持续显示。
- Agent 自动流式解析现象、定位区域、匹配台账和核对依据。
- 设备信息卡、告警状态卡按状态机依次生成，未生成的卡片完全隐藏。
- 每一项都由用户确认或修改，系统不替代工程师作最终确认。
- 已完成卡片收缩为摘要并允许重新编辑。
- 接入摘要最后生成，点击后稳定进入现有分析诊断流程。
- WSL 构建通过，并在龙芯 Node 开发态完成同模块验证后，R0 才能标记完成。

## 3. R0 当前实际完成度

| 子项 | 状态 | 代码事实 |
| --- | --- | --- |
| 工业工作台首页 | 已完成 | 已从通用输入首页重构为工业工作台形态 |
| 故障图片/视频/音频入口与预览 | 已完成（WSL） | 支持本地选择、紧凑宫格、删除、点击放大预览并带入接诊工作区 |
| 检修资料编组与持续引用 | 已完成（WSL） | 首页可从 12 个案例、17 条知识和 15 份手册中编组本次资料包；选择结果贯穿异常接入顶部、第 5 步和最终摘要，当前为前端会话态 |
| 独立设备位置识别任务 | 已完成（WSL） | 接诊 Agent 首先生成位置任务卡；确认后才生成设备信息，中央材料宫格不被挤压 |
| 接诊 Agent 状态与流式文本 | 已完成 | `App.jsx` 已有 `triageAgentStatus`、字符级输出和依据展示 |
| Agent 轻量面板与工业化样式 | 已完成 | 相关样式已实现，当前还有未提交 CSS 调整 |
| 接诊后延迟进入下一步 | 已完成（WSL） | 各确认动作均经过 Agent 运行态后生成下一项任务 |
| 单页动态接诊状态机 | 已完成（WSL） | `activeIntakeStep` 驱动位置、设备、告警任务渐进生成，未生成内容隐藏 |
| 位置图持续保留 | 已完成（WSL） | 位置确认后保留独立满铺场站图、故障点与位置标题，不覆盖材料 |
| 设备信息动态卡 | 已完成（WSL） | 支持自动识别、手动选择、确认、收缩摘要和返回修改 |
| 告警状态动态卡 | 已完成（WSL） | 支持四项告警/运行值确认、建议值和返回修改 |
| 独立接入摘要确认页 | 已完成（WSL） | 汇总现场事件、材料、位置、设备告警与运行参数，并可启动诊断 |
| 左侧流程与卡片同步 | 已完成（WSL） | 流程默认收缩为编号栏，可展开查看渐进生成节点 |
| 卡片生成原因 | 已完成基础版 | 右侧辅助对话展示已有信息、匹配过程、参考依据和下一任务生成原因 |
| 条件分支与重新评估 | 已完成（WSL） | 标准散热、无灯态高温、设备/告警不一致三条路径会改变任务提示、后续确认字段和摘要诊断方向 |
| 逐字段推荐与来源 | 已完成（WSL） | 型号、角色、关联告警按时间顺序逐项填入，并标注设备台账、现场描述/告警规则或工程师手动确认来源 |
| 分层接入摘要 | 已完成第一版 | 已使用独立摘要页组织现场事实、材料、位置、告警参数和诊断启动说明；按用户决定不做信息完整度 |
| 触发诊断回归修复 | 已验证（WSL） | 浏览器已验证摘要页点击“启动智能诊断”可稳定进入多 Agent 会诊 |
| 龙芯逐模块验证 | 未验证 | 仓库中没有本轮 R0 的龙芯运行记录或截图 |

因此，最新提交 `feat: add agent-generated intake workspace` 是 R0 的前置和局部实现，不能将整个 R0 标为完成。

## 4. 已完成并继续保留

| 功能 | 状态 | 边界 |
| --- | --- | --- |
| React + Vite 前端骨架 | 已完成 | 当前仍以单体 `App.jsx` 为主 |
| Flask 后端与静态托管 | 已完成 | 轻量演示 API |
| 单案例完整演示骨架 | 已完成 | 研华 ACP-4000 / IPC-610 散热异常 |
| 登录页与主导航 | 已完成 | 演示登录，不是真实鉴权 |
| 诊断结论页 | 已完成 | 结论来自预设数据 |
| 五步检修向导 | 已完成 | 保留内部逻辑，后续只替换数据源 |
| 检查项勾选和图片联动 | 已完成 | 当前状态主要在前端或后端内存 |
| 恢复验证 | 已完成 | 演示闭环 |
| 知识图谱基础页 | 已完成 | JSON + 前端静态布局，规模仍需扩充 |
| 专家审核基础模拟 | 已完成 | 尚未形成上传、归档、入图谱完整链路 |
| 前端离线兜底 | 已完成 | 后端不可用时仍可展示主演示流程 |

## 5. 已完成但会被新方案替换

| 旧实现 | 处理决定 |
| --- | --- |
| 固定四步异常接入翻页 | 已摒弃其页面组织方式；字段与校验逻辑保留并迁入动态补充页 |
| Agent 集中在会诊阶段展示 | 已摒弃“只集中展示”的方式；保留会诊内容，同时让 Agent 跟随业务步骤出现 |
| 固定五步作为所有案例的方案 | 主案例向导保留；跨案例固定步数被摒弃，后续由案例 JSON 和预方案确认结果决定 |
| 首页所有信息同时铺开 | 已摒弃；核心采集入口平铺，辅助信息默认折叠 |
| 仅研华品牌字段 | 已摒弃作为完整业务口径；研华案例保留，品牌库扩展到行业主流品牌 |
| 简单 8 节点图谱作为最终形态 | 已摒弃作为最终验收标准；基础实现保留并扩容 |

## 6. R2 / P0 当前实际完成度

| 子项 | 状态 | 代码事实 |
| --- | --- | --- |
| R2 产品方案与开发 Spec | 已完成 | 已新增设计文档与开发 Spec，并纳入文档导航 |
| 诊断结论到检修编排 Agent | 已完成（WSL） | 诊断结论不再直接进入向导，先运行编排过渡并生成预方案 |
| 千问式两级步骤提纲 | 已完成（WSL） | 5 个检修阶段与 23 条小维修步骤逐阶段生成，支持原位编辑 |
| 大步骤/小步骤增删改和排序 | 已完成（WSL） | 支持标题、描述、检查项修改，以及新增、删除、上下移动 |
| 修改记录与辅助对话同步 | 已完成（WSL） | 修改计数与最近 8 条工程师修改记录同步展示 |
| 强制安全项保护 | 已完成（WSL） | 正常关机、拔除所有电源、等待冷却、防静电不可删除；含锁定项的阶段不可删除 |
| 确认方案驱动检修向导 | 已完成（WSL） | 确认后重置进度，向导读取确认版本，不再被原始 API 步骤覆盖 |
| 流程栏同步 | 已完成（WSL） | 预方案阶段单独显示“当前 · 确认检修预方案”，确认后显示实际向导步骤 |
| 检修记录读取确认版本 | 已完成（WSL） | 生成记录时合并当前确认步骤及完成状态 |
| Node 20.19.4 构建 | 已通过 | Vite 生产构建通过，1584 个模块完成转换 |
| 浏览器主路径 | 已通过 | 已验证生成、编辑、新增、删除、安全锁定、确认并进入向导 |
| 龙芯开发态验证 | 未验证 | 尚无本轮 R2 的龙芯运行记录或截图 |

后续 P0：

- 统一案例库：`case-01/02/03`，不同案例具有不同步数。
- 品牌型号静态库。
- 知识回流闭环：上传 → 待审核 → 已审核归档 → 图谱出现 `new` 节点。

在 R0 完成之前，不并行扩展真实 API、数据库、图数据库或语音/视频能力。

## 7. 已明确摒弃或暂缓

### 已摒弃

- Docker 作为当前部署方式。
- 本地部署大模型。
- 用固定四步翻页解释“动态生成”。
- 用单一研华品牌代表完整油气行业设备谱系。
- 将模拟能力表述为真实识别或真实 Agent 推理。

### 暂缓

- 真实大模型 API。
- SQLite 和正式会话持久化。
- 图数据库。
- 真实图像识别定位。
- 真实语音识别与现场 TTS。
- 真实专家音视频通话。
- Word/PDF 后端导出和电子签章。
- 拖拽排序等预方案编辑增强。

## 8. 环境与验证状态

固定环境：

```text
Node.js 20.19.4
npm 10.8.2
Python 3.10+（开发环境当前为 3.12.3）
```

2026-07-10 已验证：

- WSL 使用 Node `20.19.4`、npm `10.8.2` 执行 `npm run build` 通过。
- Flask Python 编译检查通过。
- 健康、场景、诊断、向导、知识证据、图谱接口均返回 HTTP 200。
- 浏览器验证图片选择、首页材料列表、进入接诊后的中央图片展示、场站位置缩略图、删除后回退预置样例均通过。
- 浏览器验证视频选择、文件信息显示及进入接诊后的中央视频预览均通过。
- 浏览器验证首页“检修资料”打开资料编组台，完整读取 12 个案例、17 条知识和 15 份手册；采用推荐后形成 4 项资料包，并持续显示到异常接入第 5 步和最终摘要。
- 浏览器在 1280×720 视口验证资料编组台、推荐选择、首页已选状态、判断依据与最终摘要布局；控制台无本功能新增错误。
- 浏览器验证 2 张图片 + 1 个视频 + 1 个音频采用四宫格展示，材料不会铺满工作区。
- 浏览器验证点击图片打开独立预览层、按 Escape 关闭通过。
- 浏览器验证“设备位置识别”模块与现场材料区独立展示通过。
- 浏览器验证接诊 Agent 首先生成“设备位置识别”任务，确认后由位置识别 Agent 生成“补充设备信息”通过。
- 位置任务出现时中央材料宫格保持居中、不缩窄；设备信息卡已移除重复的“设备位置”字段。
- 浏览器验证左侧诊断流程默认收缩为编号栏，展开后显示完整阶段和子步骤。
- 浏览器验证位置确认后独立场站图持续保留，场站图满铺、故障点可见且不遮挡材料。
- 浏览器验证设备信息自动识别、确认、告警运行值确认和返回修改入口。
- 浏览器验证完成告警确认后进入独立“接入摘要确认页”，汇总现场事件、材料、故障位置和运行参数。
- 浏览器验证摘要页“启动智能诊断”可稳定进入多 Agent 会诊。
- 浏览器验证设备自动识别字段会逐项写入，并显示“设备台账”“现场描述 + 告警规则”等来源；手动修改后来源切换为“工程师手动确认”。
- 浏览器验证把设备改为 PLC 控制柜、关联告警改为通信中断后，下一任务从 TEMP/FAN 四项切换为控制器 RUN、通信 LINK、数据上送和电源状态。
- 浏览器验证 PLC/通信分支的任务卡、右侧 Agent 轨迹和接入摘要同步更新；摘要页会阻止该分支误入尚未建设的散热诊断结论。
- 修复摘要页“异常事件概览”修改入口的步骤映射：该卡汇总设备型号、角色和关联告警，现在直接返回第 2 步“补充设备信息”，不再错误回到初始接诊或要求重新确认位置。现场描述编辑状态仅供左侧“描述现场现象”节点使用。
- 浏览器验证诊断结论点击“生成检修预方案”后，编排 Agent 生成 5 个检修阶段和 23 条小维修步骤，左侧流程显示独立的“确认检修预方案”节点。
- 浏览器验证大步骤标题和小维修步骤可原位修改，新增/删除检修阶段可用，修改计数与右侧工程师修改记录同步更新。
- 浏览器验证 4 个强制安全项删除按钮均不可用；确认方案后，向导标题与检查项使用工程师修改后的版本。
- 浏览器控制台未发现本轮 R2 页面运行错误。

非阻断问题：开发页当前缺少 `favicon.ico`，浏览器控制台会出现 1 条 404；与本模块功能无关。

龙芯状态：尚没有本轮模块的有效验证记录，不能声称已经在龙芯完成适配。

## 9. 下一次状态更新条件

只有同时满足以下条件，才能把 R0 改为“已完成”：

1. 单页动态接诊状态机已落地。
2. 图片/视频选择、中央证据画布、Agent 过程、设备卡、告警卡和接入摘要完整串通。
3. 旧固定四步页面不再作为主流程。
4. 卡片生成原因、至少一个条件分支、逐字段来源和分层摘要通过验收。
5. 点击“启动智能诊断”能稳定进入现有会诊流程。
6. WSL 使用 Node `20.19.4` 构建通过。
7. 龙芯使用 Node `20.19.4` 开发态启动，R0 主路径操作通过。
8. 验证日期、环境版本、结果和问题已记录在本文档。
